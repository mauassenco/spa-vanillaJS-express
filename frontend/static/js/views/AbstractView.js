export default class {
    constructor(params) {
        this.params = params;
    }

    seTitle(title) {
        document.title = title;        
    }

    async getHTML() {
        return "";
    }
}